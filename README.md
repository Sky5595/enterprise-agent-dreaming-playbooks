# enterprise-agent-dreaming-playbooks
A self-improving enterprise agent loop that analyzes failed sessions, clusters failure patterns, and uses an LLM to auto-generate playbooks, prompt patches, and routing rules. Built with FastAPI, Pydantic, and Claude.
